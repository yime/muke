<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mapper.VideoMapper">

<resultMap type="Video" id="videoMap">
	<id property="id" column="id"/>
	<result property="name" column="name"/>
	<result property="chapterId" column="chapter_id"/>

</resultMap>

<select id="getVideos" parameterType="Chapter" resultMap="videoMap">

	select v.name as name, c.name as chapter_name from video v join chapter c on v.chapter_id=c.id where c.id=#{chapterId}

</select>
   
</mapper>